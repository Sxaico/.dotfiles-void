# Incluir tema catppuccin-mocha
include ~/.config/sway/themes/catppuccin-mocha

### VARIABLES ###
# Logo key. Use Mod1 for Alt. (Super/Windows)
set $mod Mod4

# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

# Terminal preferida
set $term kitty
# Launcher
#set $menu bemenu-run -cl '5 down' --fb "#1e1e2e" --ff "#cdd6f4" --nb "#1e1e2ecc" --nf "#cdd6f4" --tb "#1e1e2e" --hb "#f5c2e780" --tf "#f38ba8" --hf "#f9e2af" --af "#cdd6f4" --ab "#1e1e2ecc" -i --single-instance --scrollbar autohide -P '>' -B 1 --fn hack:size=24 -W 0.5 -R 5 --bdr "#cba6f7" -H 50 --no-overlap --accept-single --auto-select

# --- BEMENU: tema centralizado (Void/runit, sin systemd) ---
# Define una sola vez las opciones en una variable de sway y pásalas vía env al ejecutar.
# Esto evita depender de ~/.profile, ~/.bash_profile, PAM, etc.
# Opciones (idénticas a las que venías usando):
set $bmopts -cl '5 down' --fb "#1e1e2e" --ff "#cdd6f4" --nb "#1e1e2ecc" --nf "#cdd6f4" --tb "#1e1e2e" --hb "#cba6f7ff" --tf "#f38ba8" --hf "#cdd6f4" --af "#cdd6f4" --ab "#1e1e2ecc" -i --single-instance --scrollbar autohide -P 'run>' -B 1 --fn hack:size=24 -W 0.5 -R 5 --bdr "#cba6f7" -H 50 --no-overlap --accept-single --auto-select
#set $bmopts -c -l "8 down" -i --single-instance --accept-single --auto-select --scrollbar autohide --no-overlap -W 0.5 -H 50 -B 1 -R 5 --fn "Hack:size=24" -p ">" --fb "#1e1e2e" --ff "#cdd6f4" --nb "#1e1e2ecc" --nf "#cdd6f4" --tb "#1e1e2e" --tf "#f38ba8" --hb "#f5c2e780" --hf "#f9e2af" --ab "#1e1e2ecc" --af "#cdd6f4" --bdr "#cba6f7"

# usa el mismo tema $bmopts y fija XDG_CURRENT_DESKTOP=sway


set $menu bemenu-run

# Reglas para ventanas flotantes
for_window [app_id="bemenu"] floating enable, border none
for_window [app_id="blueman-manager"] floating enable, border none, opacity 1
for_window [app_id="iwgtk"] floating enable, border none, opacity 0.8
for_window [window_type="dialog"] floating enable
for_window [app_id="mpv"] inhibit_idle fullscreen
for_window [app_id="vlc"] inhibit_idle fullscreen
for_window [app_id="firefox"] inhibit_idle fullscreen
for_window [title="Bluetooth Devices"] floating enable

#for_window [window_role="dialog"] floating enable

# Start your launcher
#bindsym $mod+space exec env BEMENU_OPTS="$bmopts" $menu
#bindsym $mod+m exec env BEMENU_OPTS="$bmopts" ~/.local/bin/appmenu

# usa el mismo tema $bmopts y fija XDG_CURRENT_DESKTOP=sway
#bindsym $mod+space exec env XDG_CURRENT_DESKTOP=sway BEMENU_OPTS="$bmopts" \
#j4-dmenu-desktop --term=$term --dmenu="bemenu $BEMENU_OPTS -p 'Apps >'" --use-xdg-de
bindsym $mod+space exec env BEMENU_OPTS="$bmopts" \
  j4-dmenu-desktop --term=$term --dmenu="bemenu $BEMENU_OPTS -p 'Apps >'" --use-xdg-de


### Output configuration
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
#output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill
exec_always --no-startup-id $HOME/Scripts/wallpaper-dinamic
#
# Example configuration:
#   output HDMI-A-1 resolution 1920x1080 position 1920,0
# You can get the names of your outputs by running: swaymsg -t get_outputs
output eDP-1 resolution 1920x1080

### CONFIGURACIÓN DE ENTRADA

# Touchpad
#input "2:7:SynPS/2_Synaptics_TouchPad" {
#	dwt enabled
#	tap enabled
#	natural_scroll enabled
#    pointer_accel 0
#	}
input type:touchpad {
    dwt enabled
    tap enabled
    natural_scroll enabled
    pointer_accel 0
}

# Teclados siempre con el caps:swapescape
input "*" {
	xkb_layout "us"
	xkb_variant "intl"
	xkb_options "caps:swapescape"
	}

# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.

# Inicio:
#exec dbus-daemon --session --address=unix:path=$XDG_RUNTIME_DIR/bus
# Polkit agent sólo si está instalado
exec --no-startup-id sh -lc 'command -v lxqt-policykit-agent >/dev/null && lxqt-policykit-agent'
# Luego blueman (necesita polkit y dbus system corriendo)
exec --no-startup-id blueman-applet
exec mako
# Audio stack con guardas: nunca duplica
exec --no-startup-id sh -lc 'pgrep -x pipewire >/dev/null       || pipewire'
#exec --no-startup-id sh -lc 'pgrep -x wireplumber >/dev/null    || wireplumber'
#exec --no-startup-id sh -lc 'pgrep -x pipewire-pulse >/dev/null || pipewire-pulse'

exec_always autotiling
exec yambar
#exec blueman-applet
exec ~/.config/wcp/wcp.sh
exec udiskie --no-appindicator
exec --no-startup-id kdeconnectd
exec --no-startup-id kdeconnect-indicator

# wcp - Windows Control Panel
bindsym --no-repeat $mod+p exec "echo 'toggle visibility' > /tmp/wcp"

### CONTROLES DE VOLUMEN Y BRILLO
# Iniciar wob
set $WOBSOCK $XDG_RUNTIME_DIR/wob.sock
exec rm -f $WOBSOCK && mkfifo $WOBSOCK && tail -f $WOBSOCK | wob

# Atajos multimedia
bindsym XF86AudioRaiseVolume exec wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 5%+ && wpctl get-volume @DEFAULT_AUDIO_SINK@ | sed 's/[^0-9]//g' > $WOBSOCK
bindsym XF86AudioLowerVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%- && wpctl get-volume @DEFAULT_AUDIO_SINK@ | sed 's/[^0-9]//g' > $WOBSOCK
bindsym XF86AudioMute exec wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
#bindsym XF86MonBrightnessDown exec xbacklight -dec 5 && xbacklight -get | cut -d'.' -f1 > $WOBSOCK
#bindsym XF86MonBrightnessUp exec xbacklight -inc 5 && xbacklight -get | cut -d'.' -f1 > $WOBSOCK
#bindsym XF86MonBrightnessDown exec brightnessctl set 5%- && brightnessctl g | awk '{printf("%d\n",$1/937)}' > $WOBSOCK
#bindsym XF86MonBrightnessUp   exec brightnessctl set +5% && brightnessctl g | awk '{printf("%d\n",$1/937)}' > $WOBSOCK
# Brillo abajo
bindsym XF86MonBrightnessDown exec sh -c 'brightnessctl set 5%- >/dev/null; v=$(brightnessctl g); m=$(brightnessctl m); printf "%d\n" $(( v*100/m )) > '"$WOBSOCK"

# Brillo arriba
bindsym XF86MonBrightnessUp exec sh -c 'brightnessctl set +5% >/dev/null; v=$(brightnessctl g); m=$(brightnessctl m); printf "%d\n" $(( v*100/m )) > '"$WOBSOCK"

bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Ajustes útiles
focus_on_window_activation smart
focus_follows_mouse no
mouse_warping none
workspace_auto_back_and_forth yes

# Basics:
 # Start a terminal
 bindsym $mod+Return exec $term

 # Kill focused window
 bindsym $mod+Shift+q kill


 # Drag floating windows by holding down $mod and left mouse button.
 # Resize them with right mouse button + $mod.
 # Despite the name, also works for non-floating windows.
 # Change normal to inverse to use left mouse button for resizing and right
 # mouse button for dragging.
 floating_modifier $mod normal

 # Para lock el sistema
 bindsym Alt+Shift+l exec ~/Scripts/lock.sh
 # Reload the configuration file
 bindsym $mod+Shift+c reload

 # Exit sway (logs you out of your Wayland session)
 bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'

# Moving around:
# Move your focus around
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right
# Or use $mod+[up|down|left|right]
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move the focused window with the same, but add Shift
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right
# Ditto, with arrow keys
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Workspaces:
    # Switch to workspace
    bindsym $mod+1 workspace number 1
    bindsym $mod+2 workspace number 2
    bindsym $mod+3 workspace number 3
    bindsym $mod+4 workspace number 4
    bindsym $mod+5 workspace number 5
    bindsym $mod+6 workspace number 6
    bindsym $mod+7 workspace number 7
    bindsym $mod+8 workspace number 8
    bindsym $mod+9 workspace number 9
    bindsym $mod+0 workspace number 10
    # Move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace number 1
    bindsym $mod+Shift+2 move container to workspace number 2
    bindsym $mod+Shift+3 move container to workspace number 3
    bindsym $mod+Shift+4 move container to workspace number 4
    bindsym $mod+Shift+5 move container to workspace number 5
    bindsym $mod+Shift+6 move container to workspace number 6
    bindsym $mod+Shift+7 move container to workspace number 7
    bindsym $mod+Shift+8 move container to workspace number 8
    bindsym $mod+Shift+9 move container to workspace number 9
    bindsym $mod+Shift+0 move container to workspace number 10
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.

# Layout stuff:
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+b splith
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    bindsym $mod+s layout stacking
    bindsym $mod+w layout tabbed
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+tab focus mode_toggle

    # Move focus to the parent container
    bindsym $mod+a focus parent

# Scratchpad:
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym $mod+Shift+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym $mod+minus scratchpad show

# Resizing containers:
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Colores
# target                 title     bg    text   indicator  border
client.focused           $lavender $base $text  $rosewater $lavender
client.focused_inactive  $overlay0 $base $text  $rosewater $overlay0
client.unfocused         $overlay0 $base $text  $rosewater $overlay0
client.urgent            $peach    $base $peach $overlay0  $peach
client.placeholder       $overlay0 $base $text  $overlay0  $overlay0
client.background        $base

# Keybindings
# Screeshots
bindsym $mod+Shift+S exec grim -g "$(slurp -d)" - | wl-copy 
bindsym Print exec grim - | wl-copy
# Dolphin
bindsym Alt+E exec dolphin

# Tema gnome kde para ventanas y mouse
#set $gnome-schema org.gnome.desktop.interface
#exec_always ~/import-gsettings \
#    gtk-theme:gtk-theme-name \
#    icon-theme:gtk-icon-theme-name \
#    cursor-theme:gtk-cursor-theme-name
seat seat0 xcursor_theme Breeze_Light 24
#seat seat0 hide_cursor 5000

# Espacio entre ventanas
gaps inner 5
smart_borders on
smart_gaps on
default_border pixel 2
hide_edge_borders smart_no_gaps

# Para compatir pantalla (ej aplicaciones como zoom y discord)
#exec_always /usr/libexec/xdg-desktop-portal-gtk -r
#exec_always /usr/libexec/xdg-desktop-portal-wlr -r
#exec_always sleep 1 && /usr/libexec/xdg-desktop-portal -r
#exec_always ~/.local/bin/xdp-restart


include /etc/sway/config.d/*
